# 문서 작성 규칙

- 문서는 한국어로 작성
- 기술 용어(kube-proxy, conntrack, iptables 등)는 영문 그대로 사용
- 코드/명령어 블록은 반드시 마크다운 코드 블록으로 감싸기
- 비교 데이터는 테이블 형식으로 정리
- ADR(Architecture Decision Record)은 `docs/decisions/` 디렉토리에 `.template.md` 형식 준수
- Runbook은 `docs/runbooks/` 디렉토리에 `.template.md` 형식 준수
- 코드 변경 시 관련 문서도 함께 업데이트
